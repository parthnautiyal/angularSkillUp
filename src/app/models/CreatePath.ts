export interface CreatePath {
  about: string;
  collaboratorEmailIds: string[];
  collaboratorIds: number[];
  courseIds: number[];
  description: string;
  imageUrl: string;
  isAccessible: boolean;
  name: string;
}
