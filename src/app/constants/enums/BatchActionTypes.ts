export enum BatchActionTypes {
  LOAD_ALL_BATCHES = '[Batch] Load All Batches',
  LOAD_ALL_BATCHES_SUCCESS = '[Batch] Load All Batches Success',
  LOAD_ALL_BATCHES_FAILED = '[Batch] Load All Batches Failed',
  LOAD_BATCH_BY_ID = '[Batch] Load Batch By ID',
}
